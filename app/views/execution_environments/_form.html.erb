<%= form_for(@execution_environment) do |f| %>
  <% if @execution_environment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@execution_environment.errors.count, "error") %> prohibited this execution_environment from being saved:</h2>

      <ul>
      <% @execution_environment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :language %><br>
    <%= f.text_field :language %>
  </div>
  <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
