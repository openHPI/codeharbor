exercise_id = <%= @exercise.id %>
comment_id = <%= @comment.id %>
    commentText =  $(".comment-text[data-exercise="+ exercise_id +"][data-comment="+ comment_id +"]")
$(commentText).css("color=blue;")
$(commentText).replaceWith("<%= escape_javascript(render :partial => 'comments/edit', :locals =>{:comment => @comment, :exercise => @exercise}) %>");