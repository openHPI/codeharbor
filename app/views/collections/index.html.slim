.nav-container
  .row
    .col-md-12
      .std-heading
        i.fa.fa-folder-open style=("color: #444444")
        =< t('layouts.collections')
      - if can? :create, Group
        = link_to new_collection_path, class: 'btn btn-main float-end nav-btn-collection' do
          i.fa.fa-plus style=("color: #000066")
          =< t('collections.new.header')
br
= render 'collections'

